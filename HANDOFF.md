# Handoff moved

The Spot navigation-stack handoff now lives with the other project documents in the
`open_set_sim` superproject: `open_set_sim/docs/spot_tools_handoff.md`
(quick start: `open_set_sim/docs/spot_robot_quickstart.md`).
